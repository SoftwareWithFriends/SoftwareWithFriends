defaults: &defaults
  host: 127.0.0.1
  port: 27017

development:
  <<: *defaults
  database: software_with_friends_development

test:
  <<: *defaults
  database: software_with_friends_test

# set these environment variables on your prod server
production:
  <<: *defaults
  database: software_with_friends
  username: <%= ENV['MONGO_USERNAME'] %>
  password: <%= ENV['MONGO_PASSWORD'] %>
